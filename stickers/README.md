# Stickers

Drop `.png`, `.svg`, or `.webp` files here and they appear in the Sticker picker.

Uploading through the picker writes into this folder too, so an uploaded sticker
joins the library permanently. Served in dev by `/api/stickers` (see
`vite.config.ts`), which mirrors the `audio/` SFX library.
